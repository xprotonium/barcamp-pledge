rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /pledgeResponses/{docId} {
      // Anyone can submit
      allow write: if true;

      // Authenticated users can read everything; guests can read ONLY approved docs
      allow read: if request.auth != null || resource.data.approved == true;
    }
  }
}